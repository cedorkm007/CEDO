import { BarChart3 } from "lucide-react";

/** Placeholder — per-question results (choice counts, and n/mean/median/std-dev + distribution for Likert questions) land in Phase B of the approved plan. */
export function SurveyResultsSubtab() {
  return (
    <div className="bg-white rounded-2xl border border-[#e6ecf5] p-10 text-center text-slate-400">
      <BarChart3 size={22} className="mx-auto mb-2 text-slate-300" />
      Survey Results is coming soon.
    </div>
  );
}
