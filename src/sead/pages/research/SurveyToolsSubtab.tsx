import { ClipboardList } from "lucide-react";

/** Placeholder — the full survey builder (Multiple Choice + Likert questions, bulk upload, activity attachment) lands in Phase B of the approved plan. */
export function SurveyToolsSubtab() {
  return (
    <div className="bg-white rounded-2xl border border-[#e6ecf5] p-10 text-center text-slate-400">
      <ClipboardList size={22} className="mx-auto mb-2 text-slate-300" />
      Survey Tools is coming soon.
    </div>
  );
}
